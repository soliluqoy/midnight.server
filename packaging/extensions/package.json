{
	"name": "midnight-server-bundled-extensions",
	"private": true,
	"description": "Extensions shipped beside midnight.server.exe and loaded by default. Installed by scripts/build.ps1 with npm ci --ignore-scripts.",
	"dependencies": {
		"pi-mcp-adapter": "2.37.0"
	}
}
